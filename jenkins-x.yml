buildPack: python
pipelineConfig:
  pipelines:
    pullRequest:
      build:
        preSteps:
        - command: pip install -r requirements.txt
          agent:
            image: gcr.io/jenkinsxio/builder-python37:latest
